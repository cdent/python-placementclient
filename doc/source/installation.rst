============
Installation
============

At the command line::

    $ pip install python-placementclient

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-placementclient
    $ pip install python-placementclient
